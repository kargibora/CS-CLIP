# Default head configuration
# This is the base config for all heads

# Head configuration
type: "flexible_multi_layer"  # Default to bimodal head
mode: "bimodal"                # Default mode: bimodal alignment

# Bimodal head specific parameters
init_temperature: 1        # Initial temperature for contrastive learning
freeze_temperature: false      # Whether to freeze temperature parameter
