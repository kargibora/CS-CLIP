# Simple linear alignment head configuration
# Single modality linear alignment

# @package _global_.model.head

defaults:
  - default

# Head configuration
type: "linear"          # Registry name for simple linear alignment
mode: "alignment"       # Alignment mode for single modality
