# SugarCrepe dataset configuration
name: "SugarCrepe"
data_path: "./datasets/SugarCrepe"
subset_name: null

# Data splitting configuration
split_type: "object"
val_ratio: 0.2
split_by_tar: false

# Standard settings
use_tar_embeddings: false
use_tar_batching: false

# SugarCrepe dataset kwargs
dataset_kwargs:
  coco_root: "./datasets/SugarCrepe"
