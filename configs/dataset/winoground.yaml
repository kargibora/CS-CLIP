# Winoground dataset configuration
name: "Winoground"
data_path: "./datasets/Winoground"
subset_name: null

# Data splitting configuration
split_type: "object"
val_ratio: 0.2
split_by_tar: false

# Standard settings
use_tar_embeddings: false
use_tar_batching: false

# Winoground dataset kwargs
dataset_kwargs:
  use_auth_token: "your_token"  # Replace with actual token
