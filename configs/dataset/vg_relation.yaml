# VG Relation dataset configuration
name: "VG_Relation"
data_path: "./datasets/VG_Rel"
subset_name: null

# Data splitting configuration
split_type: "object"
val_ratio: 0.2
split_by_tar: false

# VG-specific settings
use_tar_embeddings: false
use_tar_batching: false

# VG Relation dataset kwargs
dataset_kwargs:
  download: true
