# CC3M dataset configuration
name: "CC3M"
data_path: "./datasets/CC3M"
subset_name: null

# Data splitting configuration
split_type: "object"
val_ratio: 0.2
split_by_tar: false

# Standard settings
use_tar_embeddings: false
use_tar_batching: false

# CC3M dataset kwargs
dataset_kwargs:
  combine_by_caption_id: false
