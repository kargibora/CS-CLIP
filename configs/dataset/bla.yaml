# BLA dataset configuration
name: "BLA"
data_path: "./datasets/BLA_Benchmark"
subset_name: null

# Data splitting configuration
split_type: "object"
val_ratio: 0.2
split_by_tar: false

# Standard settings
use_tar_embeddings: false
use_tar_batching: false

# BLA dataset kwargs
dataset_kwargs:
  split: "test"
