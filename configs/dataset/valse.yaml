# VALSE dataset configuration
name: "VALSE"
data_path: "./datasets/VALSE"
subset_name: null

# Data splitting configuration
split_type: "object"
val_ratio: 0.2
split_by_tar: false

# Standard settings
use_tar_embeddings: false
use_tar_batching: false

# VALSE dataset kwargs
dataset_kwargs: {}
