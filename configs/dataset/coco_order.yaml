# COCO Order dataset configuration
name: "COCO_Order"
data_path: "./datasets/COCO_Order"
subset_name: null

# Data splitting configuration
split_type: "object"
val_ratio: 0.2
split_by_tar: false

# Standard settings
use_tar_embeddings: false
use_tar_batching: false

# COCO Order dataset kwargs
dataset_kwargs:
  download: true
